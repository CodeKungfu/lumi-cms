# SQLite is the default local development database.
# You only need DATABASE_URL after switching the generated client to MySQL with:
# `pnpm --filter @repo/database db:gen:mysql`
DATABASE_URL="mysql://root:123456@localhost:3306/lumi-cms"
